export const signup = async (req,res) => {
    res.json({
        data : "you are hitting the signup endpoint"
    });
}

export const login = async (req,res) => {
    res.json({
        data : "you are hitting the login endpoint"
    });
}

export const logout = async (req,res) => {
    res.json({
        data : "you are hitting the logout endpoint"
    });
}
